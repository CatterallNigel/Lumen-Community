# M0.1 Roadmap Addendum — N9+ Praebere Requirements

**Document:** `M0.1_ROADMAP_ADDENDUM_N9_PLUS_PRAEBERE_REQUIREMENTS.md`  
**Status:** Working implementation addendum  
**Scope:** Lumen External Research Distribution M0.1 — N9 / Praebere First Native Adoption  
**Date:** 2026-08-30

---

## Revision History

| Version | Date | Author | Change |
|---|---|---|---|
| 1.0 | 2026-08-30 | Nigel Catterall | Consolidated the main-roadmap Phase 6 requirements, Nuntius N9 native-adoption objective, existing Praebere architecture/lifecycle boundaries, provider/model discovery and selection requirements, associated Servire/Nuntius/Pontis/Rogare/Moderari work, execution-evidence requirements, and currently unblocked Praebere work into one implementation checklist. |
| 1.1 | 2026-08-30 | Nigel Catterall | Defined the M0.1 provider/model selection scope as runtime-global. Per-session model selection is explicitly optional polish if time permits and remains future development if not. |
| 1.2 | 2026-08-30 | Nigel Catterall | Defined Praebere startup model discovery, preferred-versus-selected model semantics, established versus active-execution session state, runtime-global model locking while execution sessions are active, external-client selection behaviour, and Rogare model-dropdown requirements. |

---

# 1. Purpose

N9 is the first deliberate use of the completed Nuntius control plane by a service
that did **not** previously depend on legacy conversational `\obt` traversal.

The purpose is therefore larger than adding three commands to Praebere. N9 must prove
that a Lumen service can adopt the common control-plane contract natively:

```text
originator
    ↓
Pontis / Lumen control origin
    ↓
Nuntius
    ↓
Praebere
    ↓
provider/model operation
    ↓
Nuntius
    ↓
originating caller
    └── configured additional consumers only
```

The implementation must preserve the existing architectural boundary:

> **Moderari decides how the model is used. Praebere ensures the configured model
> provider is available to be used.**

Praebere owns provider/model capability and lifecycle. It must not become a
conversation, prompt, tool, Trace, Replay or assessment service.

---

# 2. Authoritative N9 Requirements

The M0.1 main roadmap defines Phase 6 as:

- [ ] Finalise `\obt providers`.
- [ ] Finalise `\obt models`.
- [ ] Finalise `\obt model select <model>`.
- [ ] Make Praebere the executor of provider/model operations.
- [ ] Return provider/model query results to the originating caller.
- [ ] Deliver discovery results to Rogare/Moderari where Servire configuration
  requires it.
- [ ] Avoid broadcasting provider/model lists to unrelated services.
- [ ] Verify selected model/provider state is represented correctly in subsequent
  execution evidence.

The Nuntius migration project adds the architectural objective:

- [ ] Praebere adopts the completed common control plane natively.
- [ ] Provider/model query and selection commands use the same Nuntius request and
  response contract already proven by migrated services.
- [ ] No legacy conversational `\obt` transport is introduced merely to support
  Praebere.

**N9 exit condition:** Rogare and external clients use the same Nuntius path for
provider/model discovery and selection, with Praebere as the authoritative executor.

---

# 3. Architectural Boundaries

## 3.1 Praebere Owns Provider Capability

Praebere owns:

- [ ] provider process lifecycle;
- [ ] provider health and reachability;
- [ ] provider ownership tracking;
- [ ] provider discovery;
- [ ] model discovery;
- [ ] model availability/state;
- [ ] model selection;
- [ ] configured/active provider and model identity;
- [ ] provider-specific lifecycle operations;
- [ ] operational logging and health for those responsibilities.

Praebere does **not** own:

- conversational sessions;
- prompts or responses;
- context management;
- system-prompt policy;
- model behavioural orchestration;
- external tool declaration/execution;
- Trace;
- Replay;
- assessment.

## 3.2 Provider-Specific Knowledge Stays in Praebere

- [ ] Do not add Ollama lifecycle/discovery logic to Moderari.
- [ ] Do not add Ollama lifecycle/discovery logic to Rogare.
- [ ] Do not add Ollama lifecycle/discovery logic to Pontis.
- [ ] Do not add provider-specific start/stop behaviour to Servire.
- [ ] Keep provider implementation details behind Praebere/provider-adapter
  boundaries.
- [ ] Model selection exposed outside Praebere describes Lumen model intent rather
  than requiring callers to understand provider endpoints or provider APIs.

## 3.3 M0.1 Provider Scope

- [ ] Treat **Ollama** as the supported M0.1 model provider.
- [ ] Preserve Ollama's expected local provider endpoint/port (`11434`) unless
  explicitly configured otherwise.
- [ ] Do not claim LM Studio or other provider support as part of M0.1 acceptance.
- [ ] Keep the architecture provider-neutral enough that another provider adapter can
  be added later without changing the Nuntius control contract.

---

# 4. Native Praebere Control Surface

## 4.1 Standard Control Endpoint

- [ ] Implement Praebere's standard Nuntius-addressable control endpoint.
- [ ] Accept the common Lumen control request envelope.
- [ ] Preserve `request_id`, `session_id`, origin and canonical/raw command metadata.
- [ ] Return common response semantics:
  - `204` — command not handled;
  - `200` without body — successful execution;
  - `200` with body — successful query/request;
  - `4xx/5xx` — recognised command failure.
- [ ] Ensure every solicited request has exactly one terminal outcome.
- [ ] Ensure success is never inferred from silence.
- [ ] Keep detailed control traffic outside conversational Trace.

## 4.2 Command Discovery

Nuntius now supports service-owned command discovery. Praebere must participate
natively rather than requiring Nuntius to know Praebere's command catalogue.

- [ ] Expose Praebere's supported `\obt` commands through its service-owned discovery
  mechanism.
- [ ] Ensure a Nuntius restart/catalogue rebuild can rediscover Praebere commands.
- [ ] Ensure command discovery reflects the commands actually implemented by the
  running Praebere version.
- [ ] Do not duplicate a hard-coded Praebere command list in Nuntius.

---

# 5. `\obt providers`

## 5.1 Semantics

- [ ] Implement/finalise `\obt providers`.
- [ ] Praebere is the authoritative executor.
- [ ] Return providers that Praebere can currently identify/use under the configured
  installation.
- [ ] Distinguish configured provider from provider availability where useful.
- [ ] Do not imply support for providers that are merely future adapter targets.
- [ ] Return a deterministic machine-usable response plus a useful human-readable
  representation where the common response contract permits it.

## 5.2 Routing

- [ ] Register Praebere as the owner of `\obt providers` in the Servire/Nuntius
  catalogue.
- [ ] Route the command directly Nuntius → Praebere.
- [ ] Return the authoritative result to the originating caller/session.
- [ ] Deliver the result to Rogare/Moderari only where Servire response-target
  configuration requires it.
- [ ] Do not broadcast the provider list to unrelated services.

---

# 6. `\obt models`

## 6.1 Semantics

- [ ] Implement/finalise `\obt models`.
- [ ] Query the active/configured provider through Praebere's provider adapter.
- [ ] For M0.1/Ollama, enumerate the models Ollama reports as locally available.
- [ ] Clearly distinguish model availability from model residency/readiness where the
  provider exposes that distinction.
- [ ] Avoid treating a currently unloaded but locally available model as nonexistent.
- [ ] Preserve provider identity with the returned model capability where required for
  unambiguous state/provenance.

## 6.2 Routing and Consumers

- [ ] Register Praebere as the owner of `\obt models`.
- [ ] Route directly through Nuntius.
- [ ] Return the model list to the originating caller/session.
- [ ] Deliver discovery results to Rogare/Moderari only where configured.
- [ ] Avoid unrelated-service broadcast.
- [ ] Ensure the same command path works for Rogare and an external client.

---

# 7. `\obt model select <model>`

## 7.1 Selection Semantics

- [ ] Implement/finalise `\obt model select <model>`.
- [ ] Validate that the requested model is available through the applicable provider.
- [ ] Reject unknown/unavailable models explicitly rather than silently retaining or
  substituting another model.
- [ ] On success, make Praebere authoritative for the selected provider/model state.
- [ ] Return explicit positive acknowledgement containing the effective selection.
- [ ] Preserve the previous valid selection if a requested selection fails.
- [ ] Make repeated selection of the already-selected model safe/idempotent.
- [ ] Permit runtime-global model selection/change only while there are **no active
  execution sessions**.
- [ ] Once the first session performs model execution, lock the selected runtime-global
  model until all active execution sessions have ended.
- [ ] Reject an attempted model change while any active execution session exists and
  return the authoritative model currently in use.

### 7.1.1 Established Versus Active-Execution Sessions

For M0.1, merely connecting a client must **not** lock model selection.

Two session states are relevant to model selection:

**Established session**

- Pontis has assigned the authoritative `session_id`;
- the session exists and may be used for control operations;
- no model interaction has yet occurred for that session;
- model selection remains possible subject to there being no other active execution
  session.

**Active execution session**

- the session has performed its first model interaction;
- its runtime model execution condition has therefore been established;
- the runtime-global selected model is locked while this or any other active execution
  session remains active.

This distinction supports the required research workflow:

```text
client connects
    ↓
Pontis establishes session_id
    ↓
model discovered/selected
    ↓
optional Trace start
    ↓
first model interaction
    ↓
session becomes active execution
    ↓
runtime-global model locked
```

The model becomes selectable again only after all active execution sessions have ended.

### 7.1.2 Preferred Model Versus Selected Model

Praebere must distinguish:

1. **available models** — models actually discovered from Ollama;
2. **preferred model** — an optional installation/UI preference;
3. **selected model** — the authoritative runtime-global model used for execution.

A preferred model is not itself an execution selection.

- [ ] Praebere queries Ollama for available models at startup.
- [ ] A configured preferred model is valid only if it exists in the discovered model
  list.
- [ ] An unavailable preferred model must not prevent Praebere/stack startup.
- [ ] Praebere must remain operational with no selected model.
- [ ] Do not silently substitute another model when the preferred model is unavailable.
- [ ] Do not automatically apply the preferred model to a newly connected external
  client.

## 7.2 Selection Intent Versus Provider Implementation

A caller should be able to select a model without knowing how Praebere implements the
provider operation.

The caller may express:

```text
\obt model select qwen2.5-coder:14b-32k
```

but should not need to express:

```text
provider = ollama
endpoint = 127.0.0.1:11434
load via provider-specific API
```

- [ ] Keep provider endpoint/API knowledge inside Praebere.
- [ ] Do not make Rogare or external clients encode provider implementation details.
- [ ] Keep the Nuntius command/response contract provider-neutral.

---

# 8. Provider and Model Runtime State

Praebere must continue to distinguish:

1. provider process existence;
2. provider API reachability;
3. model local availability;
4. model residency/readiness;
5. preferred model identity;
6. authoritative runtime-global selected model identity;
7. model-selection lock state.

- [ ] Do not collapse these states into a single `running` flag.
- [ ] Ensure provider/model query responses expose enough state to diagnose a cold or
  unloaded model without implying provider failure.
- [ ] Ensure model selection state survives normal subsequent requests for the
  runtime-global M0.1 scope.
- [ ] Expose whether model selection is currently unlocked or locked by active execution sessions.
- [ ] Re-enable selection only when no active execution sessions remain.
- [ ] Ensure stale state is not reported after provider failure/restart.
- [ ] Reconcile state from the provider when local Praebere state and provider reality
  disagree.

---

# 9. Ollama Lifecycle Ownership

Existing Praebere lifecycle behaviour remains an N9 invariant.

- [ ] If Praebere started Ollama, Praebere may stop it during the corresponding
  lifecycle shutdown.
- [ ] If Ollama was already running independently, Praebere must not stop it unless
  configuration explicitly grants that ownership.
- [ ] Servire requests provider lifecycle behaviour through Praebere; Servire must not
  kill/manage Ollama directly.
- [ ] Preserve the existing extended Praebere shutdown allowance in Servire where
  required by real provider shutdown behaviour.
- [ ] Do not hide a provider-shutdown defect by continually increasing Servire
  timeouts.
- [ ] Verify discovery and model selection work both when Praebere starts Ollama and
  when it adopts an already-running Ollama instance.

---

# 10. Readiness and Idle-Eviction Behaviour

Praebere architecture already distinguishes model availability from readiness. N9
should implement everything currently unblocked while preserving the intended future
request-orchestration boundary.

- [ ] Provide/retain a provider-neutral Praebere operation capable of ensuring the
  selected/configured model is ready where this is already within the current
  Praebere implementation scope.
- [ ] Detect when Ollama remains healthy but the selected model has become non-resident
  after idle eviction.
- [ ] Allow Praebere to reload/warm the selected model without changing the selected
  model identity.
- [ ] Keep the decision to gate an inference request on readiness with Moderari, not
  Rogare or Pontis.
- [ ] Keep the implementation of readiness with Praebere.
- [ ] Do not make completion of broader automatic Moderari readiness gating a blocker
  for N9 unless the current Praebere code already exposes the required boundary and
  integration is safely unblocked.

**Future integration boundary:** a later Moderari integration may call Praebere
`ensure-ready` before forwarding inference. That must reuse Praebere's provider-neutral
capability rather than adding provider-specific knowledge to Moderari.

---

# 11. Rogare Integration

- [ ] Rogare obtains the available model list through the Nuntius/Praebere control path.
- [ ] Rogare does not query Ollama directly.
- [ ] Display available models in a dropdown associated with session start/stop controls.
- [ ] If an available preferred model is configured and no authoritative model is
  already selected/locked, show the preferred model as the initial Rogare dropdown
  choice.
- [ ] A preferred-model dropdown choice must not be confused with authoritative
  runtime selection; selection becomes authoritative only through the Praebere
  `\obt model select <model>` path.
- [ ] When no active execution sessions exist, allow the researcher to change the
  dropdown selection.
- [ ] Before model execution/session activation, establish the chosen model through
  Praebere and wait for authoritative success.
- [ ] Once any active execution session exists, disable model choice and display the
  authoritative runtime-global model currently in use.
- [ ] If Rogare starts/restarts while another execution session is active, recover the
  authoritative model and show it as locked rather than offering a new choice.
- [ ] Rogare updates visible selected-model state only after authoritative success.
- [ ] Failed selection remains visibly failed and does not imply the requested model
  became active.
- [ ] Existing M0.1 tool-provider behaviour remains separate from model-provider
  selection.

The already-established M0.1 tool boundary remains unchanged:

- [x] external clients own their own tool declaration, execution and result handling;
- [x] Rogare has no native tool environment and uses an external tool provider;
- [x] Pi is the supported M0.1 tool provider for Rogare in its expected/default
  installation location;
- [x] alternative Rogare tool providers are not claimed for M0.1.

# 12. Moderari Integration

Moderari must consume provider/model authority without acquiring provider lifecycle
knowledge.

- [ ] Deliver provider/model discovery results to Moderari only where Servire
  configuration explicitly requires it.
- [ ] Ensure Moderari can obtain the authoritative selected provider/model state needed
  for subsequent model execution.
- [ ] Remove or avoid duplicated provider/model authority in Moderari where the N9
  implementation makes it redundant.
- [ ] Do not move Ollama start/stop/discovery code into Moderari.
- [ ] Ensure changing the selected model affects subsequent new model execution as
  intended.
- [ ] Enforce the M0.1 runtime-global selection scope consistently across concurrent
  sessions.
- [ ] Ensure a model selection made from one session is reflected as the authoritative
  model for subsequent requests from every session.
- [ ] Do not imply that Moderari's session-scoped system-prompt policy makes provider
  or model state session scoped.

**M0.1 decision:** provider and selected-model state are **runtime-global**. Praebere
maintains one authoritative provider/model selection for the local Lumen runtime.

Model selection/change is permitted only while there are no active execution sessions.
An established session that has not yet performed model interaction does not by itself
lock selection. Once the first model interaction occurs, the selected model is locked
until all active execution sessions have ended. New concurrent sessions inherit that
already-established runtime model and cannot change it.

This is deliberately simpler than per-session model selection and prevents one
concurrent researcher from changing another session's model execution condition.
The UI, control responses and execution provenance must make this scope explicit.

**Optional polish / future development:** if N9 is completed with sufficient time and
the change remains bounded, per-session model selection may be investigated. It is not
an N9 or M0.1 blocker and otherwise remains future development.

---

# 13. Servire Integration

Servire remains lifecycle/topology authority, not provider executor.

- [ ] Configure Praebere as Nuntius-addressable and `obt_enabled`.
- [ ] Configure Praebere's standard control endpoint.
- [ ] Configure response targets for provider/model discovery deliberately.
- [ ] Ensure response-target configuration can deliver results to Rogare/Moderari
  without unrelated broadcast.
- [ ] Keep Praebere's service-owned UI exposed through the Servire portal where
  applicable.
- [ ] Preserve the current alphabetical managed-component/tab ordering.
- [ ] Preserve Praebere-specific graceful provider shutdown behaviour/time allowance.
- [ ] Ensure routine provider polling does not refill the normal Servire Operational
  Log with successful internal `/api/...` noise.

---

# 14. Nuntius Integration

Nuntius routes; it does not become a provider registry.

- [ ] Obtain Praebere ownership/endpoint metadata from Servire.
- [ ] Discover Praebere's supported command catalogue from Praebere.
- [ ] Route `providers`, `models`, and `model select` to Praebere by service ownership.
- [ ] Preserve origin/session/request correlation.
- [ ] Record request lifecycle in Nuntius diagnostics.
- [ ] Return exactly one terminal result.
- [ ] Deliver result bodies only to the originator and configured additional
  consumers.
- [ ] Do not embed provider-specific behaviour in Nuntius.
- [ ] Ensure Nuntius restart/catalogue rebuild restores the Praebere route without
  requiring a full stack restart.

---

# 15. Pontis / External Client Integration

- [ ] Existing client-originated `\obt` interception must recognise Praebere-owned
  commands without adding special provider logic to Pontis.
- [ ] Preserve stable client/session/request correlation.
- [ ] Return provider/model query and selection responses to the correct originating
  client.
- [ ] Keep the command outside ordinary model ask/answer processing.
- [ ] Ensure provider/model control commands do not enter model context.
- [ ] Ensure provider/model control traffic remains outside Vestigare conversational
  Trace.
- [ ] On a newly established external-client session, if there is **no authoritative
  selected/locked model**, return Praebere's discovered available-model list plus
  guidance to use:

```text
\obt praebere model select <model_name>
```

- [ ] Do not automatically select the configured preferred model for an external
  client.
- [ ] Do not permit ordinary model execution from that external session until a valid
  model has been selected.
- [ ] If a model is already authoritative because an active execution session exists,
  do not offer a model choice. Inform the new client which runtime-global model is in
  use and that its session will use that model.
- [ ] Explain that model selection becomes available again only when no active
  execution sessions remain.
- [ ] Do not make simple connection/session establishment itself lock model selection.

Pontis remains a routing/session boundary. Model discovery and model-selection
authority remain with Praebere and travel through Nuntius; Pontis must not query
Ollama or maintain a competing model catalogue.

# 16. Execution Evidence and Provenance

Selection is not complete merely because a UI says a model is selected.

- [ ] Subsequent execution evidence identifies the effective model.
- [ ] Subsequent execution evidence identifies the effective provider where needed for
  reproducibility.
- [ ] Trace/replay evidence distinguishes requested selection from effective execution
  state where those differ.
- [ ] A failed model selection cannot cause later evidence to claim the failed model
  was used.
- [ ] Provider/model identity is captured from authoritative runtime state rather than
  inferred only from a UI selection.
- [ ] Existing system-prompt provenance and Replay fidelity remain unaffected by the
  provider/model-control migration.

---

# 17. Repetere Requirements Unblocked by N9

The earlier Repetere pass deliberately deferred provider/model fidelity work until
Praebere became authoritative.

After N9 establishes that authority:

- [ ] Define how a source Trace's effective provider/model identity is supplied to a
  Replay run.
- [ ] Ensure Replay does not silently substitute the currently selected model when the
  experimental condition requires the source model.
- [ ] Support deliberate model variation as an explicit experimental condition rather
  than accidental runtime drift.
- [ ] Run at least one same-Trace/same-model Replay after N9 to verify provider/model
  fidelity.
- [ ] Run at least one deliberate different-model Replay to verify the variation is
  explicit and evidenced.
- [ ] Preserve isolated Replay-session behaviour established in N6/N7.
- [ ] Keep failed staged-Replay recovery work tracked separately; N9 must not overwrite
  or obscure failed-run evidence.

These items may require a subsequent Repetere implementation pass; they are included
here so N9 does not close without identifying the work it unlocks.

---

# 18. Configuration and Documentation Tidy-Up

While Praebere is open for N9:

- [ ] Review Praebere's canonical configuration filename and structure for consistency
  with current Lumen conventions.
- [ ] Remove obsolete hard-coded model assumptions where native discovery/selection
  makes them redundant.
- [ ] Treat any configured model name as an optional **preferred model**, not a required installed/default model.
- [ ] Keep preferred-model configuration explicit and safe for the M0.1 Ollama-only boundary.
- [ ] Ensure absence of the preferred model does not prevent startup.
- [ ] Update Praebere architecture/user documentation to describe the native Nuntius
  control path.
- [ ] Update Servire configuration examples for Praebere ownership, endpoint and
  response targets.
- [ ] Update the main M0.1 roadmap as N9 items are completed.
- [ ] Update the Nuntius migration project checklist when N9 acceptance is complete.
- [ ] Do not append `_UPDATED`, `_RECONCILED` or similar suffixes to canonical
  documentation filenames; use revision-history tables.

---

# 19. N9 Implementation Sequence

## N9.1 — Current Praebere Audit

- [x] Inspect current Praebere provider adapter/lifecycle code.
- [x] Inspect current provider/model configuration and state representation.
- [x] Identify existing discovery/model-selection functionality that can be reused.
- [x] Identify duplicated provider/model knowledge in associated services.
- [x] Confirm the M0.1 model-selection scope: **runtime-global**.

## N9.2 — Native Control Contract

- [ ] Add the standard Praebere control endpoint.
- [ ] Add service-owned command discovery.
- [ ] Register Praebere ownership in Servire/Nuntius.
- [ ] Implement common request/response/correlation semantics.

## N9.3 — Provider Discovery

- [ ] Implement/finalise `\obt providers`.
- [ ] Return authoritative provider state.
- [ ] Validate originator and configured-consumer delivery.

## N9.4 — Model Discovery

- [ ] Implement/finalise `\obt models`.
- [ ] Enumerate Ollama local models.
- [ ] Distinguish availability/readiness appropriately.
- [ ] Validate Rogare/external-client use of the same path.
- [ ] Query Ollama on Praebere startup and maintain authoritative available-model state.

## N9.5 — Model Selection

- [ ] Implement/finalise `\obt model select <model>`.
- [ ] Validate requested model.
- [ ] Update authoritative Praebere selection.
- [ ] Propagate authoritative state to required consumers.
- [ ] Verify failed selection preserves prior valid state.
- [ ] Implement established-session versus active-execution-session selection rules.
- [ ] Lock model selection while any active execution session exists.
- [ ] Verify preferred model remains a preference and is not silently selected for external clients.

## N9.6 — Runtime/Readiness Reconciliation

- [ ] Reconcile selected/configured state against provider reality.
- [ ] Verify externally started versus Praebere-started Ollama ownership.
- [ ] Verify idle/non-resident model behaviour.
- [ ] Implement currently unblocked provider-neutral readiness capability.

## N9.7 — Execution Evidence

- [ ] Verify subsequent execution uses the selected model.
- [ ] Verify effective provider/model identity is evidenced.
- [ ] Verify no control message enters conversational Trace/model context.
- [ ] Verify system-prompt provenance remains unchanged.

## N9.8 — Associated UI/Service Validation

- [ ] Rogare discovery and selection.
- [ ] Rogare reconnect state recovery.
- [ ] Moderari authoritative model-state consumption.
- [ ] Nuntius diagnostics/correlation.
- [ ] Servire response-target behaviour.
- [ ] External-client query/selection round trip.

## N9.9 — Repetere Follow-On Boundary

- [ ] Execute the provider/model fidelity checks that N9 directly unblocks.
- [ ] Record any Repetere code changes still required for the later Replay pass.
- [ ] Do not expand N9 into unrelated Experiment/failed-run UI work.

## N9.10 — Quality Gates

For every modified service:

- [ ] `pytest`
- [ ] required coverage threshold
- [ ] `ruff check .`
- [ ] `mypy src tests`
- [ ] startup/lifecycle smoke test
- [ ] live cross-service acceptance test

---

# 20. N9+ Acceptance Scenarios

N9+ should not be closed on unit tests alone. At minimum validate:

1. [ ] Start Servire and the managed stack normally.
2. [ ] Confirm Praebere queries Ollama and discovers the locally available models.
3. [ ] Confirm absence of the configured preferred model does not fail Praebere/stack startup.
4. [ ] Confirm Nuntius discovers Praebere and its native command catalogue.
5. [ ] Issue `\obt providers` from Rogare and receive the authoritative response.
6. [ ] Issue `\obt providers` from an external client and receive the response in the
   correct originating session.
7. [ ] Issue `\obt models` and confirm the returned list matches Ollama's locally
   available models.
8. [ ] Confirm provider/model lists are not broadcast to unrelated services.
9. [ ] Establish an external-client session while no model is selected; confirm the
   available models and `\obt praebere model select <model_name>` guidance are returned.
10. [ ] Confirm an external client does not silently inherit the configured preferred
    model when there is no authoritative runtime selection.
11. [ ] Select an available model before first model execution and confirm positive
    acknowledgement identifies the effective selection.
12. [ ] Make the first model request and verify the selected model is actually used,
    evidenced, and the session becomes an active execution session.
13. [ ] While that execution session remains active, attempt to select a different model
    and verify the change is rejected with the current authoritative model reported.
14. [ ] Start a second external or Rogare session while the first remains active and
    confirm it is told which model is in use and is not offered a model choice.
15. [ ] Verify both concurrent execution sessions use/report the same locked
    runtime-global model while their session-scoped Moderari policies remain independent.
16. [ ] End all active execution sessions and verify model selection becomes available again.
17. [ ] In Rogare with no active execution sessions, confirm the model dropdown is
    populated from Praebere discovery and an available preferred model is shown as the
    initial choice.
18. [ ] Start/restart Rogare while another execution session is active and verify the
    dropdown shows the authoritative model and is disabled/locked.
19. [ ] Attempt to select an invalid model and verify explicit failure with the previous
    valid selection preserved.
20. [ ] Restart Rogare and verify authoritative selected provider/model state is recovered.
21. [ ] Restart Nuntius while the stack remains active and verify the Praebere route and
    command catalogue are rediscovered.
22. [ ] Verify Nuntius diagnostics correlate a provider/model request from origin to
    Praebere and terminal outcome.
23. [ ] Verify control commands/results remain outside model context and Vestigare
    conversational Trace.
24. [ ] Verify an independently running Ollama is not stopped by Praebere when Praebere
    does not own it.
25. [ ] Verify an Ollama instance started by Praebere is stopped according to Praebere's
    lifecycle ownership policy.
26. [ ] Verify a locally available but non-resident model is represented correctly and
    can be made ready without changing selection identity.
27. [ ] Run the same source Trace against the same effective model and confirm Replay
    provider/model fidelity where currently supported.
28. [ ] Run an explicitly selected different model and confirm the experimental variation
    is visible in evidence.
29. [ ] Run all modified-service quality gates cleanly.

# 21. N9+ Exit Condition

N9+ is complete when:

1. Praebere is a first-class native Nuntius control-plane participant;
2. `\obt providers`, `\obt models`, and `\obt model select <model>` are authoritative
   Praebere operations;
3. Rogare and external clients use the same control path;
4. results return to the correct originator and only configured additional consumers;
5. provider/model implementation details remain encapsulated by Praebere;
6. selected/effective provider-model state is runtime-global for M0.1, model changes
   are allowed only before active execution begins or after all execution sessions end,
   and that scope/lock state is accurately reflected in UI, control responses and
   subsequent execution evidence;
7. provider lifecycle ownership remains correct for Praebere-started versus externally
   started Ollama;
8. control traffic remains outside model context and conversational Trace;
9. Nuntius diagnostics provide correlation and terminal outcome evidence;
10. all modified-service tests, Ruff and mypy checks are clean; and
11. provider/model fidelity work thereby unblocked in Repetere is either completed
    where practical or explicitly carried forward with no ambiguity.

At that point N9 has proved the intended architectural point: a new Lumen capability
can adopt the common control plane directly, without first acquiring a legacy
conversational command path.

---

# 22. Work After N9

After N9 acceptance:

- update the main M0.1 roadmap;
- update the Nuntius migration project status;
- reconcile any Praebere architecture/user documentation;
- retain any unfinished Repetere provider/model fidelity work in its existing roadmap
  addenda;
- proceed to N10 compatibility removal/regression validation when the remaining
  service-specific M0.1 work permits it.

